server:
  port: 8182
spring:
  application:
    name: GraphQL searching
  datasource:
    url: jdbc:postgresql://localhost:5434/race
    password: admin123
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  graphql:
    graphiql:
      enabled: true

